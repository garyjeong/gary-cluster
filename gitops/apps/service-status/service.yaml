apiVersion: v1
kind: Service
metadata:
  name: service-status-service
  namespace: gary-app
  labels:
    app: service-status
    managed-by: argocd
spec:
  selector:
    app: service-status
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP

