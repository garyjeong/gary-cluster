apiVersion: v1
kind: Service
metadata:
  name: household-ledger-service
  namespace: gary-app
  labels:
    app: household-ledger
    managed-by: argocd
spec:
  selector:
    app: household-ledger
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
