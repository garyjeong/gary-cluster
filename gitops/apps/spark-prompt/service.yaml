apiVersion: v1
kind: Service
metadata:
  name: spark-prompt-service
  namespace: gary-app
  labels:
    app: spark-prompt
    managed-by: argocd
spec:
  selector:
    app: spark-prompt
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
